#include<bits/stdc++.h>
using namespace std;
int main() {
    int t; cin>>t;
    while (t) {
        t--;
        int n, x; cin >> n >> x;
        vector<int> nums;
        while (n) {
            n--;
            int a;cin>>a;
            nums.push_back(a);
        }
        
    }
    return 0;
}